# チュートリアルデータ仕様書（default.json）

このドキュメントは、`default.json`（および同じ形式のチュートリアルデータファイル）の記法・スキーマを定義します。

---

## 1. 全体構造

```json
{
  "format": "cube-tutorial-v1",
  "version": 5,
  "levels": [ /* レベルの配列 */ ]
}
```

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `format` | string | 必須 | 固定値 `"cube-tutorial-v1"` |
| `version` | number | 必須 | データの版数。**内容を更新するたびに必ずインクリメントしてください**（画像のキャッシュ制御や、アプリ側の進捗管理に使われます） |
| `levels` | array | 必須 | レベルの配列（下記） |

---

## 2. レベル（level）

チュートリアル全体の大きな区切り（例：「初心者（LBL法）」）です。

```json
{
  "id": "beginner",
  "title": { "ja": "初心者（LBL法）", "en": "Beginner (LBL)" },
  "steps": [ /* ステップの配列 */ ]
}
```

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `id` | string | 必須 | レベルの識別子。**同じレベル配列内で一意である必要があります** |
| `title` | string または `{lang: text}` | 必須 | レベルのタイトル。多言語オブジェクト推奨（3節参照） |
| `steps` | array | 必須 | ステップの配列（下記） |

---

## 3. ステップ（step）

レベル内の1つの学習単位（例：「下(黄色)クロス」）です。

```json
{
  "id": "step1-yellow-cross",
  "title": { "ja": "下(黄色)クロス", "en": "Bottom Cross" },
  "description": { "ja": "説明文", "en": "Description" },
  "image": "LBL/goals/step1-goal.png",
  "items": [ /* アイテムの配列 */ ]
}
```

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `id` | string | 必須 | ステップの識別子。**同じレベル内で一意である必要があります**（他のレベルのステップと同じidを使っても問題ありません） |
| `title` | string または `{lang: text}` | 必須 | ステップのタイトル |
| `description` | string または `{lang: text}` | 任意 | ステップの説明文。改行したい場合は文字列中に `<br>` を含めてください（それ以外のHTMLタグは解釈されず、そのまま文字として表示されます） |
| `image` | string | 任意 | ステップの完成図・目標状態を示す画像のパス |
| `items` | array | 必須 | アイテムの配列（下記） |

### 多言語対応（`title`・`description`共通）

```json
{ "ja": "日本語", "en": "English", "zh": "中文", "fr": "Français", "ko": "한국어", "es": "Español", "pt": "Português", "de": "Deutsch", "ru": "Русский" }
```

対応言語は上記9言語です。全言語を用意できない場合、単純な文字列（例：`"title": "テスト"`）を渡しても構いません（その場合は全言語で同じ文言が表示されます）。一部の言語だけ抜けている場合は、英語→日本語の順にフォールバックします。

---

## 4. アイテム（item）

ステップ内の個々の学習コンテンツです。`type`によって`"lesson"`（レッスン）と`"exam"`（確認テスト）の2種類があります。

### 4.1 レッスン（`type: "lesson"`）

決められた初期状態から、決められた手順をガイド付きで実行させるコンテンツです。

```json
{
  "id": "step1-lesson-1a",
  "type": "lesson",
  "title": { "ja": "基本手順（コマンドなし）", "en": "Basic (no command)" },
  "initialState": "（54文字の状態文字列）",
  "moves": "R U R' U'",
  "hint1": "LBL/step1-1b-hint.png",
  "hint2": "LBL/step1-1b-hint2.png"
}
```

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `id` | string | 必須 | アイテムの識別子。**同じステップ内で一意である必要があります** |
| `type` | string | 必須 | `"lesson"` |
| `title` | string または `{lang: text}` | 必須 | アイテムのタイトル |
| `initialState` | string | 必須 | 開始時のキューブ状態（54文字、kociemba形式。5節参照） |
| `moves` | string | 必須 | 実行させる手順（6節「movesの記法」参照） |
| `hint1` | string | 任意 | 画面上部に表示する主ヒント画像のパス |
| `hint2` | string | 任意 | 「+」ボタンで開く追加ヒント画像のパス |

### 4.2 確認テスト（`type: "exam"`）

初期状態と完成状態だけを与え、ユーザーに自由に操作させて完成を待つコンテンツです。

```json
{
  "id": "step1-exam-1",
  "type": "exam",
  "title": { "ja": "試験1", "en": "Exam 1" },
  "initialState": "（54文字の状態文字列。ワイルドカード可）",
  "goalState": "（54文字の状態文字列）",
  "hint1": "LBL/step1-exam1-hint.png"
}
```

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `id` | string | 必須 | アイテムの識別子。同じステップ内で一意 |
| `type` | string | 必須 | `"exam"` |
| `title` | string または `{lang: text}` | 必須 | アイテムのタイトル |
| `initialState` | string | 必須 | 開始時のキューブ状態（ワイルドカード対応。5節参照） |
| `goalState` | string | 必須 | 合格と判定する完成状態（54文字、ワイルドカード不可） |
| `hint1` / `hint2` | string | 任意 | ヒント画像（レッスンと同様） |

---

## 5. 状態文字列（`initialState` / `goalState`）

キューブの状態を表す54文字の文字列です。面の並び順は `U R F D L B`（各面9文字ずつ、計54文字）。各面は中央のマスを基準に、時計回りに1〜9の番号を振った位置に対応します。

通常は `U`/`R`/`F`/`D`/`L`/`B` の面文字（大文字）で埋めますが、`initialState` に限り以下のワイルドカードが使えます：

| 記号 | 意味 |
|---|---|
| `W`（大文字） | 任意の色でよい（判定対象外） |
| `w`（小文字） | このマスの色は問わないが、内部的に「操作前の色を維持」する指定として扱われる |

`goalState` にはワイルドカードは使えません（完成判定に使うため、必ず具体的な色を指定してください）。

---

## 6. `moves` の記法

`moves` フィールドに書ける手順の記法一覧です。

| 記法 | 意味 | 例 |
|---|---|---|
| `R` `R'` `R2` | WCA標準記法（時計回り／反時計回り／180°） | `R U R' U'` |
| `R2'` | 方向付き180°（ガイド矢印を反時計回りで表示） | `U2'` |
| `Rw` `x` `y` `z` | ワイド手／キューブ全体回転 | `Rw U x` |
| `C_F3(seq)` | F面3番マスに照準を表示し、ダブルタップ（またはガイドの⏩操作）で `seq` を実行する「コマンド」を配置する | `C_F3(U R U' R')` |
| `R(F)` | ガイド矢印をF面のみに表示する（複数指定可: `R(F,U)`。指定がない場合は全ての関連面に表示） | `U(F) R(F)` |
| `CAM(θ,φ)` | 視点（カメラ）移動。θ=垂直角度、φ=水平角度（度単位） | `CAM(90,0)` |

**非対応の記法**：`U'2`、`Rw'2`（`U2'`、`Rw2'` は対応）

### `CAM(θ,φ)` の詳細

- 各`CAM(θ,φ)`は、その直後から次の`CAM`が現れるまでの区間（1つの「CAMグループ」）の視点を指定します
- 先頭に`CAM`が無い区間は標準視点（θ=65°、φ=36°）がデフォルトです
- 手を進める・戻す、いずれの方向でも同じ視点を使います
- 同じCAMグループ内で手を進める/戻す間は、視点は自動では動きません（ユーザーが手動で視点を調整していれば、それはそのまま保持されます）。次のグループに切り替わる瞬間だけ、指定した視点へ自動的に移動します

例：
```
CAM(65,36) U(F) R(F) U'(F) R'(F) CAM(65,54) U'(R) F'(R) U(R) F(R) CAM(115,36)
```

### `C_XX(seq)`（コマンド）の詳細

`C_F3(seq)` のように指定すると、以下のように内部で自動的に複数のマスへ同じコマンドがコピー登録されます（F/R/B/L面の場合）：

- **面をまたぐ回転コピー**：F3で登録すると、R3・B3・L3にも自動的に登録されます（各面から見た回転を考慮した内容に自動変換されます）
- **左右対称コピー**：F3で登録すると、F1（左右対称の位置）にも登録されます。同様にR1・B1・L1にも登録されます
- 合計で最大8マスに、面ごとに正しく変換された内容が登録されます

U面の場合は、コーナー（1,3,7,9のいずれか）またはエッジ（2,4,6,8のいずれか）の回転対称コピーが行われます。

---

## 7. 最小構成の例

```json
{
  "format": "cube-tutorial-v1",
  "version": 1,
  "levels": [
    {
      "id": "sample",
      "title": { "ja": "サンプル", "en": "Sample" },
      "steps": [
        {
          "id": "sample-step1",
          "title": { "ja": "最初のステップ", "en": "First Step" },
          "items": [
            {
              "id": "sample-lesson-1",
              "type": "lesson",
              "title": { "ja": "サンプルレッスン", "en": "Sample Lesson" },
              "initialState": "UUUUUUUUURRRRRRRRRFFFFFFFFFDDDDDDDDDLLLLLLLLLBBBBBBBBB",
              "moves": "R U R' U'"
            }
          ]
        }
      ]
    }
  ]
}
```

---

## 8. 識別子（`id`）に関する注意

`level.id` / `step.id` / `item.id` は、**それぞれ自分の直接の親の中でだけ一意であれば十分です**（例えば、異なるレベルに属する2つのステップが同じ`id`を持っていても問題ありません）。

ただし、アプリ側の進捗管理（どのレッスンを完了したか等）は、これらの`id`文字列ではなく、内部的に生成される別の識別子で行われています。データの構成（レベル・ステップの追加/削除/並び替え）を変更すると、その変更が影響する範囲の進捗がリセットされることがあります。
